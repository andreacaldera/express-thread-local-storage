# Example app using thread-local storage on express

`yarn dev`

`yarn hit-endpoints`

If everything works correctly, no logs should be seen.
If something goes bad and data in the thread-local storage is not correct, you should see a warning.
